# YOLO Models

Place `yolov8s.onnx` in this folder to enable optional YOLO object detection in gesture control.

The main hand gesture control path uses `gesture_recognizer.task` and MediaPipe Hands, so the app still works when this large ONNX file is not present.
